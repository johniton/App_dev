import 'package:flutter/material.dart';

class outdoor extends StatelessWidget {
  const outdoor({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Text('You Clicked outdoor'));
  }
}
