import 'package:flutter/material.dart';

class sofa extends StatelessWidget {
  const sofa({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Text('You Clicked sofa'));
  }
}
